import React from "react";
import { ModalBody, ModalHeader, ModalFooter, Input } from "reactstrap";

const Loading = () => {
  return <div className="refundLoadingCard">Loading</div>;
};

export default Loading;
