import React from "react";
import { ModalBody, ModalHeader, ModalFooter, Input } from "reactstrap";

const Loading = () => {
  return (
    <>
      <ModalBody>Processing Refund</ModalBody>
    </>
  );
};

export default Loading;
